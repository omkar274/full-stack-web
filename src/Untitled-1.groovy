
{"query":"{ products(first:100,channel:\"channel-pln\"){ edges{ node{ id name description slug thumbnail{ url } pricing{ priceRange{ start{ gross{ amount currency } } } } category{ name slug } } } } }"}

